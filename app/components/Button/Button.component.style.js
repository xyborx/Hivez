import {StyleSheet} from 'react-native';
import {buttonDefault, textHighlight} from '../../styles/common.style';

export default StyleSheet.create({
	default: {
		...buttonDefault
	},
	text: {
		...textHighlight
	}
});